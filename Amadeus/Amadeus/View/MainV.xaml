<Window x:Class="Amadeus.View.MainV"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Amadeus.View"
        mc:Ignorable="d"
        Title="MainV" Height="361" Width="210"
        
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}" ResizeMode="NoResize" WindowStyle="None" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Grid>
        <!--背景-->
        <Image Source="/Amadeus;component/Asset/Image/StartUpTemplate.png"/>
        
        <!--立ち絵-->
        <Image Source="/Amadeus;component/Asset/Image/kurisu_angry1.png" Margin="10,100,0,10"></Image>
        <!--利確ボタン-->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"
                    Height="30"
                    VerticalAlignment="Top" Margin="0 100">
            <Button Width="80" Height="30"
                    Margin="5 0"
                    BorderThickness="0">
                <Button.Background>
                    <ImageBrush ImageSource="/Amadeus;component/Asset/Image/okabeButon.png"/>
                </Button.Background>

            </Button>
            <Button Width="80" Height="30"
                    Margin="5 0"
                    BorderThickness="0">
                <Button.Background>
                    <ImageBrush ImageSource="/Amadeus;component/Asset/Image/makiseButton.png"/>
                </Button.Background>
            </Button>
        </StackPanel>
            
    </Grid>
</Window>
